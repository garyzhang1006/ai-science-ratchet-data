Anonymized code-and-data supplement. The Kaggle kernel scripts read their clone URL from the RATCHET_REPO environment variable (push_kernels.sh substitutes it), so no repository URL or author path appears in this archive during double-blind review. Every number in the paper is recomputed by `python -m src.analysis`, `python -m src.compose`, and `python -m src.paper_numbers --release release`.
